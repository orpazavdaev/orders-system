services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - API_URL=http://host.docker.internal:8080
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    command: npm run dev
